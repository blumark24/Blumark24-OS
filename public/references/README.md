# Design references

This folder holds approved design reference images used by the
`/design-preview/*` routes during visual reconstruction work.

## blumark-mobile-dashboard-reference.png

Reference for the Blumark24 mobile dashboard preview rendered at:

- Route: `/design-preview/blumark-mobile`
- Code:  `src/app/design-preview/blumark-mobile/page.tsx`
- Docs:  `docs/BLUMARK24_MOBILE_DASHBOARD_REFERENCE_PREVIEW.md`

The original reference was attached in the design conversation that
approved this preview. Drop the approved PNG here as
`blumark-mobile-dashboard-reference.png` so future reviewers can do a
side-by-side comparison with the rendered preview route.

This folder is preview-tooling only — nothing in production loads images
from `/public/references`.
