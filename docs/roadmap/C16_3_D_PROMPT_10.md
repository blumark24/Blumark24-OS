# C16.3-D Final Gate Note

Before merging any UI motion consumption PR, confirm the following commands are clean:

- `npx tsc --noEmit`
- `npm run lint`
- `npm run build`
- `npm run verify:isolation`
