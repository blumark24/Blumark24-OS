# C16.3-D Prompt Addendum

Use this only if the UI PR needs a smaller review scope.

Focus first on `MobileExecutiveOfficeScene.tsx` because it contains the office chips and fixed chip positions.

Do not change `CHIP_POSITIONS`.
Do not change the office image.
Do not change the room array mapping.
Only merge existing motion style values into existing clickable chip styles.
